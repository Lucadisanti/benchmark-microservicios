services:
  api-1:
    image: benchmark-api
    container_name: api-1
    environment:
      - INSTANCE_NAME=api-1
    ports:
      - "5001:5000"

  api-2:
    image: benchmark-api
    container_name: api-2
    environment:
      - INSTANCE_NAME=api-2
    ports:
      - "5002:5000"